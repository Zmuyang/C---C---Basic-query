1. 不要同时使用 cout 和 printf
2. 绝对值在 1e9范围以内的整数都可定义为int类型
3. float 有效精度为 6 ～ 7 位
4. double 有效精度为 15 ～ 16 位
5.‘a’ - ‘A’ = 32
6. C 需要添加头文件 #include <stdbool.h> 使用 bool 类型
7. 强制类型转换判断好在计算后还是过程中进行转换
8. #define 标识符 常量
9. const 数据类型 变量名 = 常量
10. i++ 先赋值后增；++i相反
11. 字符数组使用 %s 读入的时候以空格跟换行作为读入结束的标志 scanf
12. scanf 的 %c 可以读入空格跟换行
13. printf 用 %f 输出 double 
14. %md 可以用空格右补齐，使输出为m位宽度
15. %0md 0右补齐
16. getchar()  putchar() 单个字符
17. typedef 给复杂的数据类型起小名
18. C中不允许在for语句里定义变量
19. break    continue
20. 数组大小必须是整数常量，不可以是变量
21. gets() 输入一行字符串 注意：scanf完一个整数后，需要紧跟getchar()，才能接着使用gets()
22. puts() 
23. 数组作为参数，在函数中对数组元素的修改等同于对原数组元素的修改 ， 不允许作为返回类型
24. a[i] = *(a+i)
25. a+i = &a[i]